<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Mobilprog.ViewModels"
             xmlns:models="clr-namespace:Mobilprog.Models"
             x:DataType="viewmodels:ProductsListViewModel"
             x:Class="Mobilprog.Views.ProductsListPage">
    <VerticalStackLayout>
        <CollectionView ItemsSource="{Binding Products}" SelectionMode="Single" SelectionChangedCommand="{Binding GoToProductDetailsCommand}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Product">
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Text="{Binding ReadableName}" FontAttributes="Bold" />
                        <Label Grid.Column="1" Text="{Binding Quantity}" Margin="10,0" />
                        <Label Grid.Column="2" Text="{Binding Price, StringFormat='{0:C}'}" HorizontalOptions="End" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
