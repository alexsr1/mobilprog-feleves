<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Mobilprog.ViewModels"
             xmlns:models="clr-namespace:Mobilprog.Models"
             x:DataType="viewmodels:ReceiptDetailViewModel"
             x:Class="Mobilprog.Views.ReceiptDetailPage">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">
            <Label Text="{Binding Receipt.StoreName}" FontSize="Title" FontAttributes="Bold" />
            <Label Text="{Binding Receipt.Date, StringFormat='{0:dd/MM/yyyy}'}" FontSize="Medium" />
            <Label Text="{Binding Receipt.TotalPrice, StringFormat='Total: {0:C}'}" FontSize="Medium" />

            <CollectionView ItemsSource="{Binding Products}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Product">
                        <Grid Padding="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Text="{Binding ReadableName}" />
                            <Label Grid.Column="1" Text="{Binding Quantity}" Margin="10,0" />
                            <Label Grid.Column="2" Text="{Binding Price, StringFormat='{0:C}'}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
